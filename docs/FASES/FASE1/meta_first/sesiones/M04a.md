# Sesión M04a — Higiene documental previa a la ruta

- **Fecha:** 2026-09-24
- **Rama:** `mf/M04a`, creada desde `main@af1babd`
- **Estado:** cerrada; `G-DOCS` aprobado por el usuario el 2026-09-24

## Bloqueo inicial y redefinición

Al leer, apareció una contradicción: `af1babd` había quitado `docs/ROADMAP.md` del árbol, pero los pasos 1 y 5 de M04a lo editaban. Además, `AGENTS.md`, `PROJECT_STATE.md`, los dos README, las plantillas y los agentes `.claude/agents/*` lo seguían citando como fuente vigente. También había dos versiones de la Enmienda 1: la de `f3aa0fe` (ROADMAP v2.4, del 2026-09-22) y la del plan v1.0. Las líneas 82 y 92 del plan corresponden a la v2.3 de `247cae8`.

Se frenó con estado `BLOQUEADO`. El usuario respondió que el ROADMAP se quitó a propósito, porque no lo sigue por ahora (opción C), y aprobó:

1. Redefinir M04a: el paso 1 queda sin efecto y el paso 5 cambia. Ya no se incorpora la Enmienda en el ROADMAP: la Parte I del plan pasa a ser el roadmap vigente.
2. Tocar archivos fuera de la tabla original: `README.md`, `docs/README.md`, `docs/_templates/microfase.md`, `.claude/agents/auditor-gate.md` y `.claude/agents/revisor-microfase.md`. Después de la primera revisión, también `.env.example`: solo el comentario de la línea 85, sin tocar variables.
3. Trasladar a `AGENTS.md` el "Contrato de ejecución para asistentes de IA" del ROADMAP.

La redefinición quedó registrada en `plan.md`, en la ficha de M04a y en II.1.

## Pasos

| Paso | Acción | Archivos | Resultado |
|---:|---|---|---|
| 1 | Sin efecto: el ROADMAP ya no está en el árbol | — | La búsqueda por patrón `act_[0-9]{6,}` no devuelve resultados (ver abajo) |
| 2 | Hallazgos de III.7 registrados. H-E1-02 y H-E1-03 ya existían con el mismo contenido: se actualizaron y se agregaron del H-E1-04 al H-E1-13. Se agregó H-E1-14 (fuentes activas que citaban el ROADMAP borrado). Se corrigió el enlace de H-E1-01, que apuntaba a `ROADMAP.md` | `docs/HALLAZGOS.md` | Cada hallazgo tiene ID, impacto, evidencia y microfase asignada |
| 3 | Nueva jerarquía, con la Parte I del plan primera, rutas `docs/FASES/FASE1/<MF>/` y contrato de ejecución trasladado. La plantilla se corrigió igual | `AGENTS.md`, `docs/_templates/prompt-implementacion.md` | 0 referencias a `docs/microfases` |
| 4 | La cita del nombre anterior del roadmap se aclaró | `docs/FASES/FASE1/MF04/m04-1-auditoria.md` | — |
| 5 | Se quitó la exigencia de comitear la Enmienda en el ROADMAP. Las menciones históricas pasan a decir "v2.3 (archivado)" | `plan.md`, `spec.md`, `README.md`, `docs/README.md`, `docs/_templates/microfase.md`, `.claude/agents/auditor-gate.md`, `.claude/agents/revisor-microfase.md`, `.env.example` | Ninguna fuente activa cita `ROADMAP.md` como vigente |
| 6 | Se usó `git mv` para mover `MFMETA/plan (2).md` → `meta_first/plan.md`, `MFMETA/spec (2).md` → `meta_first/spec.md` y `MFMETA/revision-codigo.md` → `meta_first/revision-codigo.md`. `MFMETA/` quedó vacía | `docs/FASES/FASE1/meta_first/` | Los enlaces relativos resuelven |
| 7 | Estado actualizado | `docs/PROJECT_STATE.md` | Refleja la ruta, la fuente vigente y la próxima microfase (M05.1.1) |

## Comandos y resultados reales

| Comando | Resultado |
|---|---|
| `grep -rlE 'act_[0-9]{6,}' docs AGENTS.md README.md CLAUDE.md .claude \| wc -l` | `0` |
| `git ls-files -co --exclude-standard \| xargs grep -lE 'act_[0-9]{6,}' \| wc -l` | `0` |
| `git show 247cae8:docs/ROADMAP.md \| grep -nE 'act_[0-9]{6,}'` (solo los números de línea) | Líneas 82 y 92: el dato sigue en el historial |
| `grep -rn "ROADMAP\.md"` en las fuentes activas | Todas las menciones lo describen como archivado o histórico |
| `grep -rn "docs/microfases"` en `AGENTS.md`, plantillas, agentes y README | `0` |
| Verificación de enlaces relativos de los archivos editados | Todos resuelven. El único enlace roto, `H-E1-01 → ROADMAP.md`, se corrigió |
| Montos: `grep -lE '(\$\|ARS\|USD\|US\$)\s?[0-9]{1,3}([.,][0-9]{3})+'` sobre todo el árbol | `0` archivos |
| Nombre del negocio: se copiaron las líneas 82 y 92 de `247cae8:docs/ROADMAP.md` a un archivo temporal del scratchpad, fuera del repo, y se buscaron (1) las líneas literales y (2) cada término en mayúscula de 4 o más letras en todos los `.md` del árbol. Solo se registraron conteos; el archivo temporal se borró | 0 líneas literales; 0 de 9 términos presentes |
| `npm run lint` | Sin errores |
| `npm run typecheck` | Sin errores |
| `npm run test` | 6 archivos y 63 pruebas pasaron |
| `npm run verify` | Exit 0; el build terminó sin errores |

La búsqueda se hizo siempre por patrón o por conteo, sin escribir el valor real en la terminal ni en la evidencia (trampa conocida de II.1).

## Revisión (`revisor-microfase`)

Iteración 1. Observaciones y cómo se resolvieron:

- **Bloqueante:** `.env.example:85` citaba `docs/ROADMAP.md`. El usuario autorizó corregirlo y se corrigió.
- Faltaba evidencia sobre montos y sobre el nombre del negocio. Se agregaron las dos búsquedas a la tabla de comandos.
- `plan.md`, paso 1: la verificación ahora cubre todo el árbol.
- `HALLAZGOS.md`: la evidencia de H-E1-08 y H-E1-11 ahora cita las fuentes de la spec. H-E1-08 queda marcado como hipótesis.
- Plantilla `prompt-implementacion.md`: la precondición de `gate.md` se cambió por "gate aprobado en `PROJECT_STATE.md`".
- `spec.md`, sección 15: las rutas desactualizadas se marcaron como resueltas (H-E1-02).
- `m04-1-auditoria.md:5`: se aclaró que no hay que confundir la v2.x con el `docs/ROADMAP.md` v1 que citan sus veredictos.
- `PROJECT_STATE.md`: estado final actualizado.
- **Sin cambio y pasa a pendiente del usuario:** `AGENTS.md` exige una spec "APROBADA" y la spec dice `FINAL PARA EJECUCIÓN`. Si el usuario aprueba `G-DOCS`, conviene pasar el estado de la spec a `APROBADA`. No lo hace el asistente, porque las aprobaciones son del usuario.
- **Informativo, fuera de alcance:** `src/app/(app)/app/integraciones/page.tsx:53` dice "roadmap del repositorio" sin citar `ROADMAP.md`. No se cambió.

## Desvíos

- Redefinición de los pasos 1 y 5 y archivos extra: aprobados por el usuario (ver arriba).
- Se creó `docs/FASES/FASE1/meta_first/sesiones/`, la ruta de evidencia que espera el procedimiento de microfases.

## Correcciones posteriores a la revisión de código

El 2026-09-24 se corrigieron dos hallazgos de la revisión de `mf/M04a`:

- `H-E1-15`: `plan.md` y `spec.md` ahora registran `G-DOCS` aprobado, `M04a` cerrada y `M05.1.1` como siguiente microfase habilitada. La tabla de intervenciones de la spec también distingue el gate completado de la aprobación pendiente de `M03a`.
- `H-E1-16`: `docs/_templates/microfase.md` dejó de usar enlaces relativos que se rompían al copiar la plantilla; las referencias canónicas se expresan como rutas del repositorio.

Verificación posterior:

| Comando | Resultado |
|---|---|
| `rg` sobre los textos obsoletos y los enlaces relativos defectuosos | 0 coincidencias |
| `git diff --check` | Sin errores |
| `npm run verify` | Exit 0; lint, typegen, typecheck, 6 archivos y 63 pruebas, y build de Next completados correctamente |

## Pendientes del usuario

- ~~Aprobar `G-DOCS`~~: aprobado el 2026-09-24. La spec pasó a `APROBADA`.
- Decidir la visibilidad del repositorio y la reescritura del historial, porque versiones anteriores de `docs/ROADMAP.md` conservan los datos de la cuenta piloto (`H-E1-04`).
- ~~Commit de la rama~~: hecho a pedido del usuario.
