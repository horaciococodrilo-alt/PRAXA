# Revisión de implementación 2 — M05.1.2 a M05.1.4 (K02 a K04)

## Datos del estado revisado

- **Fecha:** 2026-09-26
- **Rama:** `mf/M05.1.2-M05.1.4`
- **Commit:** `5e4c9c0` (HEAD coincide con `main`; todo el cambio sigue sin commitear)
- **Hash del estado** (excluye los informes de revisión): `1b268fdbd37b80a4da2bb9195e2b34b4ba8ef6c9`
- **Precondiciones:** rama correcta; `docs/PROJECT_STATE.md` y la sesión marcan `VERIFICADO — PENDIENTE DE APROBACIÓN` (`G-K02-K04`). Existe ya `sesiones/M05.1.2-M05.1.4-implementation-review-1.md` (veredicto **REQUIERE CAMBIOS** por R-01 y R-02), así que este es el informe número 2, sobre el estado corregido. No hay carpeta de pipeline `docs/FASES/FASE1/M05.1.2-M05.1.4/`, así que este informe va en `sesiones/`.

## Veredicto

**APROBABLE.** Los 8 ítems del checklist están en PASS. R-01 y R-02, señalados por la revisión de implementación 1, están corregidos, probados con pruebas propias (no solo con las que ya existían) y confirmados por mutación. No se encontró ningún hallazgo nuevo de severidad alta o media.

## Checklist

| # | Ítem | PASS/FAIL | Evidencia |
|---|---|---|---|
| 1 | Alcance | PASS | 9 archivos tocados: los 5 de `src/modules/integrations/contract/` y `tests/unit/integration-contracts.test.ts` (tabla II.3), más `docs/HALLAZGOS.md`, `docs/PROJECT_STATE.md` y el archivo de sesión, que autoriza la precondición 8 del contrato de ejecución. `git diff main -- supabase/` vacío. La corrección de R-01/R-02 solo tocó `primitives.ts` y el archivo de pruebas, como declara la sesión; confirmado leyendo el diff completo |
| 2 | Criterios | PASS | Cada CA de las tres fichas (M05.1.2 a M05.1.4) tiene al menos una prueba cuya aserción lo verifica, incluidas las 4 pruebas nuevas de R-01/R-02. 61 de 61 pasan (ver Matriz) |
| 3 | Las pruebas detectan roturas | PASS | 3 de 3 mutaciones propias detectadas (AAD sin `connection_id`, DTO con `company_id`, transición no declarada `pending_selection → needs_reauth`); los 3 archivos restaurados y `cmp` confirmó que son idénticos a la copia previa a la mutación |
| 4 | Reejecución | PASS | `npx vitest run --project unit tests/unit/integration-contracts.test.ts`: 61 de 61, igual que la sesión. `npm run verify`: **exit 0**, 8 archivos y 145 pruebas, igual que la verificación final de la sesión. Corrida desde Git Bash (`/c/Users/Simon/dev/PRAXA`): no se reprodujo el fallo intermitente de `H-E1-18`/`H-E1-20` en esta sesión de revisión tampoco |
| 5 | Seguridad y reglas | PASS | Búsqueda por patrón (`EAA…`, `act_[0-9]{6,}`, `eyJ…`, `sk-…`, `[0-9]{10,}`) sobre las líneas `+` de `git diff main -- src/modules/integrations tests/unit/integration-contracts.test.ts`: 0 coincidencias. Ninguna lectura de `.env.local`, ningún uso de `process.env` ni `service_role` en el módulo. `git diff main -- supabase/migrations` vacío. `server-only` no corresponde: el módulo es un contrato puro sin I/O, igual que `reporting/contract` (confirmado: tampoco lo usa). Errores redactados con `redactErrorMessage`, ahora con las correcciones R-01/R-02 |
| 6 | Calidad funcional | PASS | Los caminos de error de la spec están implementados (error tipado de intento, rechazo de transiciones, esquema de mensaje redactado con las dos correcciones). Sin `any` ni `as any` en el módulo (grep sin coincidencias). Sin TODO/FIXME/XXX. Sigue el patrón de `reporting/contract` (DTO por construcción, sin `omit`) |
| 7 | Evidencia | PASS | La sesión y la revisión de implementación 1 registran comandos, resultados reales y desvíos. `PROJECT_STATE.md` describe el estado como `VERIFICADO — PENDIENTE DE APROBACIÓN`, sin marcar el gate como aprobado. Los hallazgos (`H-E1-18`, `H-E1-19`, `H-E1-20`) tienen ID y están en `docs/HALLAZGOS.md` |
| 8 | Contradicciones | PASS | Sin contradicciones con la spec, el plan ni `AGENTS.md`. La desviación de columnas de K02 respecto de II.4 y el TTL propio de 10 minutos están en `H-E1-19`, asignados correctamente (M06.1a y decisión del usuario en el gate) |

## Matriz de criterios

| Criterio | Prueba (describe › caso) | Aserción verifica el criterio (sí/no) | Resultado |
|---|---|---|---|
| CA-01 | K02 › rechaza el state en claro; rechaza state_hash inválido; el state generado tiene ≥32 bytes | sí | pasa |
| CA-02 | K02 › rechaza vencimiento ausente; rechaza vencimiento largo o anterior a la creación; vencido/consumido no consumible | sí | pasa |
| CA-02b | K02 › rechaza vinculación al navegador ausente/inválida; rechaza intento sin actor/empresa; cookie HttpOnly/Secure/SameSite=Lax | sí | pasa |
| CA-02c | K02 › initial sin conexión/generación esperadas; reauth las exige | sí | pasa |
| CA-03 | K02 › vencido/consumido no consumible; rechazo como error tipado único, sin detalles internos | sí, en la parte de aplicación (atomicidad real es de pgTAP en M06.2a, declarado como límite) | pasa |
| CA-04 | primitivas › allowlist acepta solo rutas declaradas; K02 › rechaza URL externa/fuera de allowlist | sí | pasa |
| CA-05 | primitivas › enum cerrado; K02 › rechaza proveedor distinto de `meta` | sí | pasa |
| CA-06 | K03 DTO › campos exactos; serialización sin campos de credencial; esquema rechaza campo de credencial; `expectTypeOf` (verificado por `tsc`) | sí | pasa |
| CA-07 | primitivas › matriz 4×4 de transiciones; `disconnected` sin salida; K03 › rechaza estado desconocido | sí | pasa |
| CA-08 | K03 › exige moneda/zona/cuenta en `active`/`needs_reauth`, incluido el caso de los tres nulos a la vez; rechaza moneda/zona inválida; `pending`/`disconnected` admiten nulos | sí | pasa |
| CA-09 | primitivas › redacción de URL, pares `=`/`:`, JSON, Bearer, credenciales embebidas; truncado con 30 desplazamientos; idempotencia; esquema rechaza mensaje sin redactar. K03/DTO › error con URL con parámetros no llega sin redactar | sí | pasa |
| CA-09b | K03 › guarda `client_business_id` y cuenta externa; DTO › cuenta sale truncada; **R-01**: cuenta en la ruta de una URL no llega al DTO sin redactar | sí, incluida la ruta de URL que la revisión 1 encontró como hueco | pasa |
| CA-10 | K04 › ningún campo de texto plano (esquema y `expectTypeOf`) | sí | pasa |
| CA-11 | K04 › IV de 12, etiqueta de 16, versión positiva; cada campo obligatorio ausente invalida | sí | pasa |
| CA-11b | K04 › AAD combina empresa/conexión/proveedor; rechaza entradas inválidas | sí | pasa |
| CA-11c | K04 › tipo de token, app emisora, permisos, expiración nullable | sí | pasa |
| CA-12 | Fixtures sintéticos + búsqueda por patrón repetida en esta revisión | sí | pasa |
| CA-13 | K04 › credencial sin conexión no valida; cada campo ausente | sí | pasa |
| CA-32 | K04 › permisos concedidos incluyen `ads_read`; rechaza duplicados | sí | pasa |
| CA-39 (restricción `needs_reauth`) | K03 › solo las 4 clases de CA-39 llevan a `needs_reauth` | sí | pasa |
| R-02 (`clave: valor`) | primitivas › redacta pares con dos puntos, idempotente | sí | pasa |

## Roturas

Copias de respaldo en el scratchpad de esta sesión, fuera del repositorio.

| Criterio | Rotura | Prueba que la detectó | Restaurado (cmp) |
|---|---|---|---|
| CA-11b (aislamiento: AAD ligado a la conexión) | `credential.ts`: `buildCredentialAad` dejó de incluir `connection_id` en el AAD | K04 › "el AAD combina empresa, conexión y proveedor y cambia si cambia cualquiera (CA-11b)": 1 falla | sí, idéntico |
| CA-06 (aislamiento: el DTO no expone datos internos de otra empresa) | `connection.ts`: se agregó `company_id` al esquema del DTO y a `toConnectionDto` | K03 DTO › "el DTO tiene exactamente los campos declarados" y "la serialización del DTO no contiene…": 2 fallan | sí, idéntico |
| CA-07 (estados: solo las transiciones declaradas) | `primitives.ts`: se agregó la transición no declarada `pending_selection → needs_reauth` | primitivas › "solo las transiciones declaradas están permitidas (CA-07)": 1 falla | sí, idéntico |

Después de restaurar las tres: `npx vitest run --project unit tests/unit/integration-contracts.test.ts` dio 61 de 61, y `git status --porcelain` coincide con la foto inicial.

## Comandos reejecutados

| Comando | Resultado |
|---|---|
| `npx vitest run --project unit tests/unit/integration-contracts.test.ts` (Git Bash, cwd `/c/Users/Simon/dev/PRAXA`) | 61 de 61 pasaron |
| `npm run verify` (Git Bash, mismo cwd) | **Exit 0.** Lint sin errores; typegen y `tsc --noEmit` en verde; 8 archivos y 145 pruebas pasaron; `next build` compiló sin errores. Coincide con la verificación final de la sesión |
| Búsqueda por patrón de secretos/identificadores sobre las líneas `+` de `git diff main -- src/modules/integrations tests/unit/integration-contracts.test.ts` | 0 coincidencias |
| `git diff main -- supabase/migrations` | vacío |
| 3 mutaciones (AAD, DTO, transición) | 3 detectadas, 3 restauradas y confirmadas con `cmp` |

III.3 no exige suites adicionales para `M05.1.1` a `M05.1.4`. Ninguna prueba quedó sin ejecutar (CB-06).

## Hallazgos

Ninguno nuevo. Los registrados por la sesión y por la revisión de implementación 1 (`H-E1-18`, `H-E1-19`, `H-E1-20`) siguen vigentes en `docs/HALLAZGOS.md`, con la asignación correcta, y R-01/R-02 están corregidos y verificados (ver Roturas y Comandos reejecutados de la sesión, confirmado de forma independiente en esta revisión).

## Pendientes del usuario

- Aprobar `G-K02-K04`.
- Ratificar o cambiar el vencimiento máximo de 10 minutos del intento OAuth (`H-E1-19`).
- Decidir si `H-E1-20` (observación de entorno, Git Bash vs. PowerShell) amerita seguimiento adicional; en esta revisión tampoco se reprodujo.
- No hay suites que dependan del usuario para esta microfase.
