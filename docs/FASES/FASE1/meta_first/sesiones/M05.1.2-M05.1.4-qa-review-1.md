# QA y verificación final 1 — M05.1.2 a M05.1.4 (K02 a K04)

## Datos del estado revisado

- **Fecha:** 2026-09-26
- **Rama:** `mf/M05.1.2-M05.1.4`
- **Commit:** `5e4c9c0` (HEAD coincide con `main`; el cambio de la microfase sigue sin commitear)
- **Hash del estado** (excluye los informes de revisión, calculado con el comando del protocolo): `1b268fdbd37b80a4da2bb9195e2b34b4ba8ef6c9`
- **Precondiciones:** rama correcta. La última revisión de implementación (`sesiones/M05.1.2-M05.1.4-implementation-review-2.md`) es **APROBABLE**, y su hash de estado (`1b268fdbd37b80a4da2bb9195e2b34b4ba8ef6c9`) coincide con el hash recalculado en esta sesión: **no está BLOQUEADO**, no cambió código desde esa revisión.
- **Foto inicial de `git status --porcelain`** (para comparar después de las pruebas exploratorias):
  ```
   M docs/HALLAZGOS.md
   M docs/PROJECT_STATE.md
  ?? docs/FASES/FASE1/meta_first/sesiones/M05.1.2-M05.1.4-implementation-review-1.md
  ?? docs/FASES/FASE1/meta_first/sesiones/M05.1.2-M05.1.4-implementation-review-2.md
  ?? docs/FASES/FASE1/meta_first/sesiones/M05.1.2-M05.1.4.md
  ?? src/modules/integrations/
  ?? tests/unit/integration-contracts.test.ts
  ```
  No hay carpeta de pipeline `docs/FASES/FASE1/M05.1.2-M05.1.4/`, así que este informe y `pr.md` van en `sesiones/`.

## Veredicto

**LISTO PARA PR.**

Todos los CA de las fichas M05.1.2 a M05.1.4 tienen al menos una prueba que los verifica, `npm run verify` pasa completo (III.3 no exige suites adicionales para este rango), las 5 pruebas exploratorias de borde no encontraron ningún defecto nuevo, y los pendientes del usuario que quedan (ratificar el TTL de 10 minutos, decidir sobre `H-E1-20`, aprobar el gate) son los propios del cierre del gate `G-K02-K04`, no bloqueos de esta verificación: las tres fichas declaran explícitamente `Intervención requerida del usuario: Ninguna`.

## Matriz de criterios

| Criterio | Caso | Tipo | Cubierto por | Resultado |
|---|---|---|---|---|
| CA-01 (state solo como hash, ≥32 bytes) | `state` en claro llega al esquema | negativo | `K02 › rechaza el state en claro` | pasa |
| CA-01 | `state_hash` con forma no SHA-256 | negativo | `K02 › rechaza un state_hash que no es un SHA-256 en hex` | pasa |
| CA-01 | `state`/nonce generado con 32 bytes exactos y con más | positivo/borde | `K02 › el state generado tiene 32 bytes o más` | pasa |
| CA-02 (vencimiento explícito y corto) | vencimiento ausente o `null` | negativo | `K02 › rechaza el vencimiento ausente` | pasa |
| CA-02 | vencimiento posterior al máximo o anterior a la creación | negativo | `K02 › rechaza un vencimiento largo o anterior a la creación` | pasa |
| CA-02 | vencimiento exactamente igual al TTL máximo (`OAUTH_ATTEMPT_MAX_TTL_MS`) | borde, positivo | **exploratoria QA** (no existía en la suite) | pasa |
| CA-02/CA-03 | intento vencido, consumido, o exactamente en el instante de vencimiento | borde | `K02 › un intento vencido o consumido no es consumible` | pasa |
| CA-02b (vinculación navegador/actor/empresa) | hash de navegador ausente o inválido | negativo | `K02 › rechaza la vinculación al navegador ausente o inválida` | pasa |
| CA-02b | intento sin actor o sin empresa | negativo | `K02 › rechaza el intento sin actor o sin empresa` | pasa |
| CA-02b | cookie con los atributos correctos | positivo | `K02 › la cookie de vinculación es HttpOnly, Secure y SameSite=Lax` | pasa |
| CA-02c (propósito) | `initial` sin conexión/generación esperadas | positivo | `K02 › un intento initial no lleva conexión ni generación esperadas` | pasa |
| CA-02c | `reauth` sin conexión o generación esperada | negativo | `K02 › un intento reauth exige conexión y generación esperadas` | pasa |
| CA-03 (consumo único y atómico) | error tipado, sin exponer cuál condición falló | positivo | `K02 › el rechazo de un intento es un único error tipado, sin detalles internos` | pasa (atomicidad real en base: pgTAP, M06.2a — límite declarado) |
| CA-04 (allowlist de retorno) | ruta declarada | positivo | `primitivas › la allowlist de retorno acepta solo rutas declaradas` | pasa |
| CA-04 | URL absoluta, protocolo relativo, `..`, consulta, fragmento, ruta no listada | negativo | misma prueba + `K02 › rechaza una URL de retorno externa o fuera de la allowlist` | pasa |
| CA-04 | ruta válida con una barra final agregada (`/app/integraciones/`) | borde, negativo | **exploratoria QA** (no existía en la suite) | pasa |
| CA-05 (proveedor cerrado) | proveedor distinto de `meta` | negativo | `K02 › rechaza un proveedor distinto de meta` | pasa |
| CA-06 (DTO sin campos de credencial, por construcción) | campos exactos del DTO | positivo | `K03 DTO › el DTO tiene exactamente los campos declarados` | pasa |
| CA-06 | serialización sin `company_id`/`credential_generation`/cuenta completa | negativo (mutación) | `K03 DTO › la serialización del DTO no contiene…`; rotura M6 de la sesión y rotura de la revisión de implementación 1 (DTO con `company_id`) | pasa |
| CA-06 | el DTO es un tipo distinto del registro, no un `omit` | borde (tipos) | `K03 DTO › el DTO es otro tipo, no el registro filtrado` (`expectTypeOf`, verificado por `tsc`) | pasa |
| CA-07 (transiciones declaradas) | las 4×4 transiciones posibles | positivo/negativo | `primitivas › solo las transiciones declaradas están permitidas` | pasa |
| CA-07 | `disconnected` sin salida | borde | `primitivas › disconnected no tiene salida` | pasa |
| CA-07 | transición no declarada (`pending_selection → needs_reauth`) | negativo (mutación) | rotura de la implementation-review-2 | pasa |
| CA-08 (moneda/zona/cuenta obligatorias en active/needs_reauth) | los tres nulos a la vez | borde | `K03 › exige moneda, zona y cuenta en active y needs_reauth` | pasa |
| CA-08 | moneda o zona con formato inválido, incluida forma de desplazamiento fijo (`+03:00`) | negativo | `K03 › rechaza moneda o zona inválidas fuera de pending_selection` | pasa |
| CA-08 | `pending_selection`/`disconnected` con metadatos nulos | positivo | `K03 › en pending_selection los metadatos pueden ser nulos…`; `K03 › disconnected exige la marca de purga y admite metadatos nulos` | pasa |
| CA-09 (error como clase + mensaje redactado) | URL con consulta/fragmento/credenciales embebidas, pares `clave=valor`, `clave: valor`, JSON, Bearer | negativo | `primitivas ›` (batería de 7 pruebas de redacción) | pasa |
| CA-09 | truncado a 500 sin dejar marcador a medias, e idempotencia | borde | `primitivas › trunca sin dejar un marcador a medias y sigue siendo idempotente` | pasa |
| CA-09 | palabras comunes con la subcadena `act_` (`contact_id`, `react_native`, `impact_report`) no se enmascaran de más | negativo (falso positivo) | **exploratoria QA** (la sesión afirmó esto "verificado por lectura del patrón", sin prueba) | pasa |
| CA-09b (cuenta truncada en el DTO) | cuenta en la ruta de una URL de error (R-01) | negativo | `K03 DTO › un error con la cuenta en la ruta de una URL no llega al DTO sin redactar` | pasa |
| CA-09b | `maskExternalAccountId` con id largo (20 car.) y corto (3 car.) | positivo/borde | `K03 DTO › el DTO muestra la cuenta truncada` | pasa |
| CA-09b | `maskExternalAccountId` en el borde exacto (8 vs. 9 caracteres) | borde | **exploratoria QA** (no existía en la suite) | pasa |
| CA-10 (solo cifrado, sin texto plano) | campos `token`/`access_token`/`plaintext`/`secret`/`code` agregados | negativo | `K04 › no existe ningún campo de texto plano` | pasa |
| CA-11 (IV, etiqueta, versión de clave) | IV/etiqueta de largo incorrecto, `key_version` cero o fraccionario | negativo | `K04 › exige IV de 12 bytes, etiqueta de 16 y versión de clave positiva` | pasa |
| CA-11b (AAD con empresa/conexión/proveedor) | AAD cambia si cambia empresa o conexión | positivo | `K04 › el AAD combina empresa, conexión y proveedor…` | pasa |
| CA-11b | AAD con `company_id` inválido o proveedor desconocido | negativo | `K04 › el AAD rechaza entradas inválidas…` | pasa |
| CA-11b | AAD sin `connection_id` (aislamiento) | negativo (mutación) | rotura de la implementation-review-2 | pasa |
| CA-11c (tipo de token, app emisora, permisos, expiración) | `expires_at` presente o `null`, `token_type` desconocido | positivo/negativo | `K04 › guarda tipo de token, app emisora, permisos y expiración nullable` | pasa |
| CA-12 (sin fixtures con forma de token real) | búsqueda por patrón sobre el diff completo | negativo | búsqueda repetida en esta sesión (ver Comandos) | pasa, 0 coincidencias |
| CA-13 (credencial referencia una conexión) | `connection_id` ausente, vacío o no-UUID | negativo | `K04 › una credencial sin conexión no valida` | pasa |
| CA-32 (exige `ads_read`) | permisos sin `ads_read`, permisos duplicados | negativo | `K04 › los permisos concedidos incluyen ads_read` | pasa |
| CA-39 (restricción de clases en needs_reauth) | las 9 clases de error, solo 4 llevan a needs_reauth | positivo/negativo | `K03 › solo las clases de CA-39 que piden reautorizar llevan a needs_reauth` | pasa |

## Pruebas exploratorias

Archivo temporal: `tests/unit/__qa__/M05.1.2-M05.1.4-qa.test.ts`. Solo interfaz pública (`@/modules/integrations/contract`). Corridas con `npm run test:unit -- tests/unit/__qa__/`.

| Caso | Resultado |
|---|---|
| `maskExternalAccountId('abcd1234')` (8 caracteres exactos) no muestra ningún dígito | pasa: devuelve `'…'`, como predice `length > 8` en `connection.ts` |
| `maskExternalAccountId('abcd12345')` (9 caracteres) muestra los últimos 4 | pasa: devuelve `'…2345'` |
| Un intento con `expires_at` exactamente en `created_at + OAUTH_ATTEMPT_MAX_TTL_MS` es válido | pasa: la condición del contrato es `ttl > 0 && ttl <= OAUTH_ATTEMPT_MAX_TTL_MS` (inclusiva), y el borde no estaba probado en la suite existente (solo `MAX_TTL_MS + 1`) |
| `redactErrorMessage` sobre `contact_id_123`, `react_native_error`, `impact_report_failed` no toca el texto | pasa: las tres palabras salen intactas, confirmando de forma independiente la afirmación no probada de la sesión sobre el límite de palabra antes de `act_` |
| `returnPathSchema` rechaza `/app/integraciones/` (barra final agregada) | pasa: rechazada, el enum de valores exactos no admite la variante |

**Resultado global: 5 de 5 pasan.** Ningún caso exploratorio encontró un defecto. Se recomienda incorporar como pruebas permanentes al menos el borde del TTL (positivo) y el de `maskExternalAccountId` en 8/9 caracteres, porque cubren una rama de código (`length > 8`) que hoy solo se ejerce indirectamente con un identificador de 20 caracteres.

Después de correr las pruebas, el archivo temporal se borró y `git status --porcelain` volvió a coincidir exactamente con la foto inicial (verificado).

## Recorridos que necesitan al usuario

Esta microfase (K02 a K04) es un contrato TypeScript/Zod puro, sin UI, sin red y sin base de datos (II.3: "Sin base de datos ni red"). No hay pantalla, endpoint ni flujo manual que ejercitar. **No hay guion manual pendiente para M05.1.2 a M05.1.4.**

## Verificación completa

| Comando | Resultado |
|---|---|
| `npm run verify` (Git Bash, cwd `/c/Users/Simon/dev/PRAXA`) | **Exit 0.** Lint sin errores; `next typegen` y `tsc --noEmit` en verde; `vitest run --project unit --project component`: 8 archivos, 145 pruebas; `next build` compiló sin errores (13 rutas) |
| `npm run test:unit -- tests/unit/__qa__/` (pruebas exploratorias) | 1 archivo, 5 pruebas, todas en verde |
| Búsqueda por patrón de secretos/identificadores (`EAA[A-Za-z0-9]{10,}`, `act_[0-9]+`, `sk-[A-Za-z0-9]{10,}`, `eyJ[A-Za-z0-9_-]{10,}`, `[0-9]{10,}`) sobre las líneas `+` de `git diff main -- src/modules/integrations tests/unit/integration-contracts.test.ts` | 0 coincidencias |
| `git diff main -- supabase/migrations` | vacío (sin migraciones tocadas, CB-03) |
| `git status --porcelain` antes y después de las pruebas exploratorias | idénticos |

Según III.3, `M05.1.1` a `M05.1.4` no exigen ninguna suite además de `npm run verify` (no hay fila para `db:check:test`, `test:policies` ni `test:app` en ese rango). Ninguna prueba requerida quedó sin ejecutar (CB-06).

## Condición del gate

Evaluación de la "Condición para avanzar" (`G-K02-K04`, ficha M05.1.4) y de la "Evidencia de cierre" de las tres fichas:

| Parte | Estado |
|---|---|
| M05.1.2 — Evidencia de cierre: "Pruebas en verde" | **Cumplida.** CA-01 a CA-05 y CA-02b/c, todas con prueba en verde |
| M05.1.3 — Evidencia de cierre: "Pruebas en verde" | **Cumplida.** CA-06 a CA-09b, todas con prueba en verde |
| M05.1.4 — Evidencia de cierre: "Pruebas en verde y barrel de exportación" | **Cumplida.** CA-10 a CA-13, CA-11b/c en verde; `src/modules/integrations/contract/index.ts` existe y reexporta los tres contratos |
| M05.1.4 — Condición para avanzar: `G-K02-K04` | **Pendiente del usuario**, por diseño: es el propio gate que este informe habilita a cerrar. No es un bloqueo de esta QA sino el paso siguiente del flujo (commit, PR, aprobación) |
| `docs/PROJECT_STATE.md` consistente con el resultado | **Cumplida.** Registra `VERIFICADO — PENDIENTE DE APROBACIÓN (G-K02-K04)`, sin marcar el gate como aprobado, y enlaza a la sesión |
| `sesiones/M05.1.2-M05.1.4.md` consistente | **Cumplida.** Mismo estado, mismos hallazgos (`H-E1-18` a `H-E1-20`), sin declarar el gate cerrado |
| `docs/HALLAZGOS.md` consistente | **Cumplida.** `H-E1-18`, `H-E1-19`, `H-E1-20` presentes, con severidad, impacto, evidencia y asignación (M06.1a o decisión del usuario en `G-K02-K04`) |

## Hallazgos

Ninguno nuevo de esta sesión de QA. Los tres hallazgos vigentes (`H-E1-18`, `H-E1-19`, `H-E1-20`) ya están registrados en `docs/HALLAZGOS.md` con ID, impacto, evidencia y microfase asignada; esta sesión no encontró motivo para modificarlos ni agregar uno nuevo. Las 5 pruebas exploratorias de borde (TTL exacto, `act_` en palabras comunes, ruta con barra final, `maskExternalAccountId` en 8/9 caracteres) pasaron sin excepción.

| ID | Severidad | Criterio | Problema | Evidencia | Corrección propuesta |
|---|---|---|---|---|---|
| — | — | — | Sin hallazgos nuevos | — | — |

## Pendientes del usuario

- Aprobar el gate `G-K02-K04` al mergear el PR.
- Ratificar o cambiar el vencimiento máximo de 10 minutos del intento OAuth (`H-E1-19`); si cambia, es un ajuste dentro de `oauth-attempt.ts` (`OAUTH_ATTEMPT_MAX_TTL_MS`) que no reabre el resto del contrato.
- Decidir si `H-E1-20` (observación de entorno Git Bash/PowerShell) amerita seguimiento adicional. No se reprodujo en esta sesión de QA tampoco (`npm run verify` corrió limpio desde Git Bash).
- No hay suites que dependan del usuario para esta microfase (III.3: ninguna adicional para `M05.1.x`), y no hay recorrido manual pendiente (ver "Recorridos que necesitan al usuario").
